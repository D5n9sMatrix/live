'  The operations “n,” “?,” and “\” and the relations “?” and “?” extend
'  directly to multisets. For example,
'  {x, x} ms ? {x} ms = {x, x, x} ms .

Declare Operator <> (ByRef x As Integer, ByRef n As Integer) As Integer

' By ignoring repetitions, a multiset can be converted to a set, while a set can be
' viewed as a multiset with distinct elements.

Do While x <> n
       return x * n                                                                         
Loop   

End


