'  for all x ? X, it follows that statement Z is satis?ed,
'  or, equivalently,
'  statement Z is satis?ed for all x ? X.
'  (1.1.4)
'  (1.1.5)

Declare Operator Let(ByRef x As Integer, ByRef y As Integer, ByRef z As Integer)

' Let X and Y be sets. The intersection of X and Y is the set of common
' elements of X and Y given by

[Let] x = y

Dim all As Integer

for  all = 1 To 10 
       return x * y / z
Next

End
